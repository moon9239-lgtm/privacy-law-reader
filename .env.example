# This public static reader does not require environment variables.
# Keep local secrets in your deployment provider or private workspace.
# Do not commit project IDs, deployment URLs, tokens, personal data, or analytics settings.
