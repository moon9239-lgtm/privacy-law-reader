# The reader itself works as a static site; these are only for the optional public visitor badge.
# Configure them in the Vercel project, never in the browser or this repository.
# VERCEL_ANALYTICS_TOKEN (server-only)
# SUPABASE_URL (server-only)
# SUPABASE_SERVICE_ROLE_KEY (server-only)
# VERCEL_TEAM_ID (optional)
# VERCEL_PROJECT_ID (optional)

